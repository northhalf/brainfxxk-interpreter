/// CLI entry point: `bf <file>` / `bf -e '<code>'` / `bf` (REPL).
///
/// Implementation lands in a later step.
void main(List<String> args) {
  // CLI implementation is completed in a later step.
}
